{
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "cli": "pnpm --filter @flightdeck-ai/server cli",
    "mcp": "pnpm --filter @flightdeck-ai/server mcp"
  }
}
